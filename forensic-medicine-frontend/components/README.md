# Shared components

The project uses a small vanilla-JavaScript templating pattern:

- `app-shell.js` renders the shared sidebar, top bar, session controls, and role-aware navigation.
- `ui.js` contains shared formatting, toast, status badge, and CSV helpers.
- `qr-modal.js` renders the shared QR label modal for case and evidence tracking.
